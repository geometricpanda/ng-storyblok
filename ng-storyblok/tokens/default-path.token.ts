import { InjectionToken } from '@angular/core';

/**
 * Injection token for the default path to use for the Storyblok API.
 */
export const NG_STORYBLOK_DEFAULT_PATH = new InjectionToken<string>('NG_STORYBLOK_DEFAULT_PATH');
